# 연세노인전문요양원 홈페이지 프로젝트

노인요양시설(요양원) 홈페이지를 기획부터 프로토타입 배포까지 진행하는 작업.
**요양병원이 아니라 요양원(노인복지법상 노인요양시설)이다.** 의료행위·치료효과를
암시하는 표현을 쓰지 않는다.

## 현재 상태 (2026-08-01 기준)

| 단계 | 상태 |
|---|---|
| 1. 정보 수집 · 기획 | 완료 → `docs/기획서.md` |
| 2. 단일 HTML 프로토타입 (14뷰) | 완료 → `prototype.html` |
| 3. 정식 HTML 문서화 · 반응형/접근성 보정 | 완료 (2026-08-01) |
| 4. 로고 반영 · 색 체계 재정의 | 완료 (근사 심볼 사용 중, 원본 파일 대기) |
| 5. 실제 사진·문구 반영 | 자료 대기 |
| 6. 실서비스 전환 — Eleventy + Decap CMS 구조 전환 | **완료 (2026-08-01)** → `src/` |
| 7. 배포 (GitHub 저장소 · Cloudflare Pages · OAuth 프록시 · 도메인) | 미착수 |

고객이 **실제 배포를 원한다**고 확인했다. 먼저 프로토타입으로 디자인을 완성한 뒤 배포한다.

## 실서비스 스택 (2026-08-01 고객 확정)

**되도록 무료 운영, 도메인만 결제.** 게시판·사진은 전부 직원이 관리자 페이지에서 직접 관리.

- 호스팅: Cloudflare Pages (무료, HTTPS 자동). 빌드 `npx @11ty/eleventy`, 출력 `_site/`
- 정적 사이트 생성기: **Eleventy 3** (`src/` 입력). 프로토타입 디자인을 그대로 이식했다
- 관리자 페이지: **Decap CMS** (`/admin/`). 공지사항·행사소식·포토갤러리 세 컬렉션.
  글은 `src/notices|events|photos/*.md` 로 저장되고 커밋 → 자동 재빌드
- **자유게시판은 우선 미운영** (2026-08-01 고객 결정). 방문자 글쓰기는 무료 구성으로
  불가능한 게 계기. 메뉴·프로토타입에서 제거했고, 필요해지면 별도 방안을 잡는다
- 지도: API 미사용. 약도 이미지 + 네이버지도/카카오맵 장소 링크로 처리 (API 키·카드등록 회피)
- 정적 사이트라 게시글 **조회수 없음** — 게시판 표에서 조회 열을 뺐다
- 방문 통계: **Cloudflare Web Analytics** (2026-08-01 설치, 쿠키 없음 → 동의 배너 불필요).
  base.njk 하단 beacon 스크립트. 대시보드는 Cloudflare 계정 → Web Analytics,
  hostname `prontfor85.github.io`. **도메인 전환 시 hostname 을 추가**해야 통계가 이어진다
- 문의는 전화 우선(성공 지표와 일치). 후원 폼은 아직 데모(`data-demo`)

### 배포 계획 (2026-08-01 변경: 2단계로)

**1차 — 무료 URL로 의뢰인 검수용 공개. 완료 (2026-08-01).**
검수용 주소: **https://prontfor85.github.io/yonsei-care-home/** (저장소
`prontfor85/yonsei-care-home`, 공개). `.github/workflows/deploy.yml` 이 push 마다
빌드·배포한다. GitHub Pages 는 `/저장소명/` 하위 경로라 워크플로가 `PATH_PREFIX` 를 넣고,
`EleventyHtmlBasePlugin` 이 절대 경로를 고쳐 쓴다.

배포 과정에서 배운 것: GitHub Desktop 퍼블리시는 기본이 비공개고(무료 Pages 는 공개
필수 — 웹 Danger Zone에서 전환), `configure-pages` 의 `enablement: true` 는 기본
토큰 권한으로 실패한다 — **Pages 소스는 Settings → Pages 에서 수동으로 "GitHub
Actions" 를 한 번 선택해야 했다.** push 는 커맨드라인에서 바로 된다 — gh CLI 를
`~/bin/gh` 에 설치(PATH 에 없음, 전체 경로로 호출)하고 사용자가 로그인해 둬서
git credential helper 가 연결돼 있다. GitHub Desktop 도 설치돼 있다(같은 계정).

**관리자 로그인 활성화 (2026-08-01 완료).** 구성: GitHub OAuth 앱 "연세요양원 관리자
로그인"(prontfor85 계정) + 자작 중계 Worker `infra/cms-auth-worker`
(https://ysc-cms-auth.cms-auth-worker.workers.dev, Cloudflare 계정 prontfor@gmail.com).
시크릿(GITHUB_CLIENT_SECRET)은 사용자가 `wrangler secret put` 으로 직접 등록 —
**시크릿 값을 대화·저장소에 두지 않는다.** 참고: sveltia-cms-auth 를 쓰려다 외부 코드
배포가 정책상 막혀 같은 역할의 Worker 를 직접 작성했다(70줄, 저장소에 포함).
편집 권한 = 저장소 쓰기 권한. 직원 계정은 GitHub 계정 생성 후 공동작업자 초대 필요.

**2차 — 도메인 결제 후 정식 전환.** Cloudflare Pages 연결(접두사 불필요, PATH_PREFIX
미설정이면 루트로 빌드됨) + 도메인 연결. 이때 **세 곳을 같이 바꿔야 로그인이 유지된다**:
`config.yml` 의 site_url / Worker 의 ALLOWED_ORIGIN / OAuth 앱의 Homepage·callback URL.
개인정보처리방침은 후원 폼 연동 시 게시.

## 파일

- `docs/기획서.md` — 타깃 사용자, 사이트맵, 페이지별 콘텐츠 설계, 법적 유의사항, 로드맵
- `src/` — **실서비스 소스 (수정은 이제 여기서).** Eleventy 입력.
  - `_includes/base.njk` — 공통 레이아웃(헤더·드로어·푸터·전화바). 메뉴 링크는 `navlink` 매크로가
    `aria-current` 를 계산하고, 상단 메뉴 활성은 각 페이지 front matter 의 `grp` 로 정한다
  - `_includes/logo.njk` — 로고 SVG 인라인 조각 / `_includes/post.njk` — 공지 상세 레이아웃
  - `css/site.css` · `js/site.js` — 프로토타입에서 추출. JS 는 해시 라우터만 제거
  - 정적 페이지 10개(`index.njk`, `about/*`, `admission`, `service`, `support/*`) +
    알림마당 목록 3개(`news/*`) + 콘텐츠(`notices|events|photos/*.md`, 현재 예시 데이터)
  - `admin/` — Decap CMS (한국어 UI, 어르신 사진 동의 안내를 힌트로 박아 뒀다)
- `prototype.html` — 디자인 원본이던 단일 HTML. **`src/` 전환 후에는 참고용.**
  해시 라우팅으로 뷰 전환(자유게시판 제거로 13뷰). 라이트/다크 테마 대응.
- `assets/logo.svg` — 고객이 준 로고 원본
- `.claude/launch.json` — 프리뷰용 `eleventy --serve` (포트 8087)

## 시설 정보 (확정)

- 시설명: 연세노인전문요양원
- 주소: 전북특별자치도 김제시 용지면 용지1길 18-4
- 전화 063-542-2737 / 팩스 063-542-2738
- 연중무휴 · 면회 09:00–12:00 / 13:00–17:00
- 지정일 2021.08.31 · 정원 25명 · 생활실 2인실 1 / 3인실 5 / 4인실 2
- 김제터미널에서 차로 약 15분, 정문 주차장
- 프로그램: 예배, 인지기능향상, 음악·신체, 미술, 전래놀이, 교구블록

전화·팩스·주소·면회시간은 고객이 직접 제공. 나머지는 장기요양기관 공개 DB 기준.

## 메뉴 구조 (고객 확정)

요양원 소개(비전/운영원칙, 인사말, 조직도, 시설현황, 오시는 길) ·
입주안내 · 사업소개(요양원) · 후원안내(후원안내, 자원봉사) ·
알림마당(공지사항, 행사소식, 포토갤러리)

원래 알림마당에 자유게시판이 있었으나 **2026-08-01 고객 결정으로 우선 미운영** — 제거됨.
같은 날 **입주안내 하위에 "면회 안내"(`/visit/`) 추가** — `src/` 에만 있고 prototype.html
에는 없다(프로토타입은 이 시점부터 참고용이라 갱신하지 않음).

## 면회 예약 (2026-08-01 방향 확정)

면회는 예약 없이 와도 받아드린다(고객 확인). 예약은 **필수가 아니라 권장 절차**로만
포지셔닝하고, 이 문구를 사이트에서 빼지 말 것. 도입은 2단계 — ① 네이버 스마트플레이스
예약(무료, 시설이 등록 후 작업자를 멤버 초대: **계정 비밀번호를 받지 않는다**) →
② 이용량이 검증되면 Supabase 자체 시스템(고객이 Vercel·Supabase 계정 보유. 단 Vercel
무료 티어는 약관상 비상업용이라 호스팅 대체재로는 쓰지 않기로 함). 상세와 고객 확인
3문항은 `docs/기획서.md` §5-1, 고객 발송용은 `docs/시설-요청자료-안내문.md`
(발송 전 **작업자 네이버 아이디 빈칸**을 채워야 한다).
`/visit/` 의 네이버 예약 버튼은 스마트플레이스 등록 전까지 `data-demo`(준비 중) 상태.

## 로고 (2026-08-01 수령 · 반영 완료)

원본: `assets/logo.svg` (고객 제공, Illustrator 출력). 원위치는
`~/Desktop/작업/연세노인전문요양원/연세노인전문요양원.svg`.

가로형 워드마크. 심볼 + "연세"(네이비) + "노인전문"(오렌지 칩, 두 줄) + "요양원"(네이비).

**심볼 구조를 오해하기 쉽다.** 십자가는 파란 면 위에 그려진 게 아니라, 파란 면(왼쪽)과
초록 면(오른쪽) **사이의 흰 여백이 십자가를 이룬다.** 수직 기둥은 두 면 사이의 틈이고,
가로 팔은 양쪽 면에 파인 홈이다. 심볼을 다시 그리거나 자를 일이 있으면 이 점을 볼 것.

`prototype.html` 헤더에 원본 path를 그대로 인라인했다(18개 path).
- 원본 viewBox `0 0 1056 318` 은 여백이 넓어서, 실제 콘텐츠 bbox(x 22.3→1034.3,
  y 73.7→244.4)에 맞춰 **`viewBox="14 66 1028 187"` 으로 잘라 뒀다. 가로세로비 5.497.**
  덕분에 높이만 정하면 된다 — 데스크톱 40px(=220px 폭), 모바일 33px(=181px 폭).
- 원본 클래스 `st0~st3` 은 충돌 위험이 있어 `l-navy / l-blue / l-green / l-orange` 로 바꿨다.
- **테마를 타는 건 글자(`l-navy`) 하나뿐이다.** `--logo-navy` 로 제어하며 다크에서 `#C9D6EC`.
  파랑·초록·오렌지는 양쪽 테마에서 원본 값 그대로 쓴다.

## 디자인 결정

- **다크 모드 제거 (2026-08-01 사용자 결정, "굳이 필요없음").** `src/` 에서 테마 토글 버튼,
  `prefers-color-scheme` 대응, `data-theme`/`localStorage['ysc-theme']` 를 전부 걷어내고
  라이트 값만 남겼다. CSS 변수 체계 자체는 유지. **이 문서의 다크 관련 서술은
  prototype.html(참고용)에만 해당한다.** 상단 "예시입니다" 배너(.proto)도 같이 제거.
- **모바일(<760px)에서 히어로 CTA 버튼 숨김 (2026-08-01 사용자 결정).** 하단 고정 바가
  전화를 담당하므로 중복 제거. 덕분에 고정 바를 숨겼다 올리던 IntersectionObserver
  로직도 통째로 삭제됐고 고정 바는 모바일에서 항상 떠 있다. "입주 절차 보기"는
  모바일에서 메뉴로만 접근한다.
- **컬러는 로고 원본 파일에서 뽑았다** (2026-08-01 변경, 기존 그린/금색 폐기).
  고객 요청은 "심볼 십자가에 있는 블루·그린을 쓰고 싶다".
  - 로고 원색 = `#1d336c` 네이비 / `#3d7fbd` 블루 / `#4d9746` 초록 / `#d36b2e` 오렌지.
    **화면에서 눈으로 뽑지 말 것** — 첫 시도에 추정했다가 전부 지나치게 채도가 높았다.
    실제 값은 훨씬 차분하고, 그게 요양원 톤에 더 맞는다.
  - 주색 `--primary` = 로고 네이비 `#1d336c` 그대로 (흰 배경 12.05:1).
  - 보조색 `--accent` = `#3B7335`. **로고 초록 `#4d9746` 은 흰 배경에서 3.65:1 이라
    본문에 못 쓴다.** 어둡게 조정한 값이며, 처음 `#3F7A39` 로 잡았다가 프로토 배너
    (초록 글자 on `--accent-soft`)가 4.49:1로 간발의 차 미달이라 한 단계 더 내렸다.
  - `--brand-blue / --brand-green / --brand-orange` 는 **로고를 그릴 때만** 쓴다.
  - `--on-primary` / `--cta-bg` / `--cta-ink` 를 둔 이유: 다크모드에서 주색이 밝아지는데
    버튼 글자가 `#fff` 로 하드코딩돼 있어 대비가 2.4:1까지 떨어졌었다. 하드코딩 금지.
  - 라이트·다크 양쪽 전 항목 WCAG AA 통과를 확인했다. 색을 바꾸면 다시 검사할 것.

### 프리뷰 캐시 주의

브라우저 프리뷰는 파일을 열 때 **스냅샷을 뜨고 재로딩하지 않는다**(쿼리스트링을 붙여도
소용없다). CSS 값을 바꾼 뒤 `getComputedStyle` 로 확인하면 옛 값이 나와 헛다리를 짚는다.
색 대비처럼 계산으로 끝나는 검증은 파일 값을 직접 읽어 계산하는 편이 빠르고 확실하다.
- 타이포: 본문 제목은 명조, **로고 워드마크는 굵은 고딕**(로고를 따른다). 본문 18px / 행간 1.8
- **한글 조판: `word-break: keep-all` 필수.** 없으면 어절 중간에서 줄이 끊긴다.
- 성공 지표는 전화 연결 → 모바일 하단 고정 전화 버튼, 전 페이지 대표전화 노출
  (760–1180px 구간은 하단 바도 헤더 버튼도 안 나와서 util 바에 `.utel` 을 따로 뒀다)

### 히어로 (2026-08-01 개편)

**사진 자리를 넣었다.** 보호자가 요양원 홈페이지에서 가장 먼저 확인하려는 건 "여기가 어떤
곳인가"인데 글자만 있으면 그 질문에 답을 못 한다. 실제 사진이 오면 `.heroshot .ph` 를
`<img>` 로 갈아끼우면 된다.

**히어로 사진은 한 장 고정 (2026-08-01 사용자 결정).** 여러 장 자동 슬라이드/크로스페이드를
검토했으나 한 장으로 확정 — 다시 제안하지 말 것. 사진 선정 기준: 외관보다 밝은 생활
공간·활동 장면이 우선(외관은 오시는 길에).

**풀블리드 배경 방식 (2026-08-01 확정, 모바일 포함, `src/` 전용).** 사진이 히어로 전체에
깔리고(`.hero.has-bg .hero-bg`) 텍스트 구간을 `--surface-2` 기반 그라데이션(`.hero-scrim`,
`color-mix` 사용)이 덮는다. 배경색 변수 기반이라 라이트/다크 자동 성립 — 흰 글자
하드코딩 방식은 쓰지 않았다(테마 대비 붕괴 전례). 두 시안(풀블리드 vs 오른쪽 배치+페이드)
중 사용자가 그라데이션이 자연스러운 풀블리드를 선택했다.
- **900px 기준으로 이미지가 갈린다** (`<picture>` + `min-width:900px` source).
  이상 = 가로형(스크림 90deg, 피사체 오른쪽) / 미만 = 세로형(스크림 180deg, 피사체
  아래쪽 — 위는 글자, CTA 아래 `padding-bottom:220px` 로 사진이 드러나는 영역 확보).
  **모바일용을 안 올리면 가로형을 겸용**하는 폴백. 사용자가 두 버전 이미지를 직접 제작 예정.
- 사진이 없으면 `.has-bg` 가 안 붙고 기존 점선 플레이스홀더로 돌아간다.
- 사진 2종·문구는 어드민 "사이트 설정 → 메인 화면"에서 편집(`src/_data/home.json`).
  히어로 문구 줄바꿈은 `nl2br` 필터가 처리. 어드민 힌트에 피사체 위치 기준을 적어 뒀다.

`.herogrid` 는 **모바일에서 자연스러운 문서 순서**(태그→제목→사진→설명→CTA)로 흐르고,
900px 이상에서만 `grid-template-areas` 로 왼쪽 글 / 오른쪽 사진 2단이 된다.
순서를 바꾸려고 마크업을 건드리지 말 것 — 모바일 순서가 기준이다.

**하단 고정 전화바는 홈 첫 화면에서 내려가 있다.** 히어로의 "입소 상담 전화하기" 버튼과
고정 바의 "전화 상담" 이 같은 역할이라, 첫 화면에 전화 버튼이 두 개 겹쳐 보였다.
`IntersectionObserver` 로 히어로 CTA를 관찰해 — 보이는 동안 `data-hidden`, 스크롤해서
벗어나면 해제한다. 히어로가 없는 나머지 페이지에서는 항상 띄운다.
홈 마크업에만 `data-hidden` 을 박아 두고 시작하는 이유는 첫 로딩에서 바가 나타났다
내려가는 깜빡임을 막기 위해서다 (`src/` 에서는 base.njk 가 front matter `hero` 로 분기).

**히어로 facts는 화면 크기에 따라 항목이 다르다.** 모바일에서는 `.f-tel`(대표전화)과
`.f-loc`(위치)을 숨긴다. 하단 고정 바가 전화·오시는 길을 이미 담당하고 있어서, 안 숨기면
첫 화면에 전화번호가 세 번(히어로 CTA · facts · 고정 바) 나온다.
**고정 바가 못 담는 것만 남긴다**가 기준이므로, 고정 바 항목을 바꾸면 여기도 같이 볼 것.
데스크톱은 고정 바가 없으니 4개 다 유지한다.

모바일 세로 예산: 제목·사진·설명·CTA가 한 화면에 들어와야 한다. 390×844에서 CTA 하단과
고정 바 사이 여유가 37px, 375×667(SE)에서도 사진 전체가 첫 화면에 들어온다.
히어로에 무언가를 더하면 이 여유부터 확인할 것.

## 종교적 성격 (2026-08-01 확인)

**기독교 기반 시설이 맞고, 자연스럽게 드러내는 방향으로 확정.** 반영한 곳:
비전/운영원칙의 "설립 정신" 블록, 인사말 한 문단, 입주안내 FAQ.

문구 원칙 — 예배는 정규 일과로 소개하되, **입소·생활의 조건이 아니라는 점을 반드시 함께
적는다.** 보호자가 "우리 부모님은 신앙이 없는데 안 받아주나" 로 읽으면 전화가 끊긴다.

## 반응형 · 접근성 (2026-08-01 적용)

- 정식 HTML 문서로 전환: `<!doctype html>` + `<html lang="ko">` + charset + **viewport**.
  이전에는 이것들이 없어 쿼크 모드로 렌더링됐고 모바일에서 데스크톱 폭으로 축소됐다.
  Artifact 배포용 조각으로 작성됐던 탓 — Artifact에 올릴 땐 head/body를 벗긴 사본을 쓸 것.
- 게시판 표는 620px 미만에서 카드로 쌓인다(`.board-wrap`). 글 번호는 숨기고 말머리만 남긴다.
- 주간 프로그램표는 시간×요일 6열이라 모바일에서 가로 스크롤이 났다.
  **요일을 행으로 뒤집어 3열로 만들어 해결.** 다른 표들도 모바일에서 `min-width` 를 푼다.
- 드로어: 포커스 가두기 + 스크롤 잠금 + 닫을 때 버튼으로 포커스 복귀.
- 테마 선택은 `localStorage['ysc-theme']` 에 저장, head의 인라인 스크립트로 깜빡임 방지.
- 14개 뷰 전부 390px에서 가로 넘침 없음을 확인했다. 레이아웃을 건드리면 다시 확인할 것.

## 채워야 할 빈칸 (프로토타입에 점선 박스로 표시됨)

0. **설립 법인 또는 교회명**, 설립 연혁 — 시설현황 표에 "확인 중"으로 비워 둠

1. **인력 현황** — 공개 DB마다 사회복지사 1↔2명, 간호사 2명↔간호조무사 1명으로
   불일치해 숫자를 비워둠. 실제 재직 인원 확인 필요. **우선순위 1번**
   (참고: 경쟁 시설 김제노인전문요양원은 직종별 인원을 전부 공개하고 있고,
   보호자 신뢰에 가장 크게 작용하는 정보다.)
2. **비급여 항목 단가** — 식재료비, 상급침실료, 이·미용비.
   장기요양기관은 비급여 항목·금액 게시 의무가 있어 실서비스 전 필수
3. 시설장 성함 · 사진 · 인사말 원고
4. 후원 계좌(은행/계좌번호/예금주), 기부금영수증 발급 가능 여부
5. 실제 사진 (외관, 생활실, 프로그램실, 식당, 행사)
6. 경유 버스 노선번호와 하차 정류장명
7. 실제 프로그램 시간표 / 하루 일과표
8. 보유 공간 실제 목록(물리치료실 등), 건물 연면적·층수

## 레퍼런스

`http://www.gimjecare.org/` — 김제노인전문요양원(별개 시설). 고객이 준 메뉴 구조의 원본.
반응형 아님, 연혁이 2013년에서 멈춤, HTTPS 없음. "이렇게는 하지 말자"의 사례.
다만 상담 연락처 상시 노출과 직종별 인원 공개는 배울 점.

## 실서비스에서 계속 지킬 것

- 개인정보처리방침 · 이용약관 게시 (후원/봉사 폼이 개인정보를 수집하므로 법적 의무 —
  폼을 실제 연동하기 전까지는 `data-demo` 상태 유지)
- 포토갤러리에 어르신 사진 게시 시 본인 또는 보호자의 서면 동의 필요
  (Decap 이미지 필드 힌트에도 적어 뒀지만, 직원 교육 때 다시 강조할 것)
